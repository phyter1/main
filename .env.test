# Test Environment Variables
# These are MOCK values for testing only - not real credentials
# This file SHOULD be committed to the repository

# AI Integration - OpenAI API
# Mock API key for testing (not a real key)
OPENAI_API_KEY=test_key_for_testing_only_sk_mock_1234567890

# AI Rate Limiting (using actual defaults to allow default-checking tests to pass)
# AI_MAX_REQUESTS_PER_MINUTE=10  # Commented to use default
# AI_MAX_TOKENS_PER_REQUEST=4096  # Commented to use default

# Admin Authentication
# Mock admin password for testing (NOT production password)
ADMIN_PASSWORD=TestPassword123!@#ForTests

# Admin session secret for encrypting session data
# Mock 64-character secret for testing (NOT production secret)
ADMIN_SESSION_SECRET=test_session_secret_64_characters_long_for_testing_purposes_only_1234

# Convex - Database for admin data storage
# Mock Convex URL for testing (NOT real Convex instance)
NEXT_PUBLIC_CONVEX_URL=https://test-convex-url.convex.cloud

# Optional: Admin rate limiting (prevents brute force attacks)
ADMIN_MAX_REQUESTS_PER_MINUTE=100

# Vercel Blob Store - Image Upload Storage
# Mock Blob token for testing (NOT real token)
BLOB_READ_WRITE_TOKEN=vercel_blob_test_token_for_testing_only_mock_value_12345
